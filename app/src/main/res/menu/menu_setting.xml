<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
  <item
      android:id="@+id/action_change_settings"
      android:title="@string/menu.setting.change_language"
      app:showAsAction="never"
      app:iconTint="#FFFFFF" />
    <item
        android:id="@+id/action_search"
        android:title="@string/menu.setting.search"
        app:showAsAction="ifRoom"
        app:iconTint="#FFFFFF"
        android:icon="@drawable/ic_search" />
    <item
        android:id="@+id/action_reminder"
        android:title="@string/menu.setting.reminder"
        app:showAsAction="ifRoom"
        app:iconTint="#FFFFFF"
        android:icon="@drawable/ic_reminder" />

</menu>